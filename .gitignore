# Rust
extension/target/
extension/*.iml
**/*.rs.bk
# Go
gateway/bin/
# Local tooling
.idea/
.vscode/
*.log
# Never commit certificates or keys (docs/RULES.md §3)
*.pem
*.key
*.crt
*.csr
*.srl
e2e/.kind/

# TLS material, if a quickstart is ever run from this directory. A private key
# must never be committed. Anchored: unanchored, this would also swallow new
# files under deploy/compose/certs/, which is tracked source.
/certs/

# Built artifacts (scripts/package-extension).
/dist/

# scripts/local-dev writes a rewritten kubeconfig here; it holds cluster
# credentials and is regenerated on every `make local-dev-up`.
.local-dev/

# Coverage data and reports (scripts/coverage-report, #90)
/coverage/
